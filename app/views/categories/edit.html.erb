<h2>カテゴリ編集</h2>

<%=  form_with model: @category, url: category_path(@category), method: :patch, local: true do |f| %>
  <%= f.label :name, "カテゴリ名:" %>
  <%= f.text_field :name %>
  <%= f.submit "変更を保存する" %>
<% end %>